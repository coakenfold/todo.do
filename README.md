# Todo.do

A todo list saved to local storage

Using React's `useReducer` hook to manage state

## Of Note

You can edit the `list` & `todo` names by clicking on the text and typing. Changes are saved by pressing `Enter`

Pressing `Escape` or tabbing away before a save resets it to the original text
